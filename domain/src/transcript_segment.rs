//! Re-exports for transcript segment data access, accessible to the web layer via `domain`.

pub use entity::transcript_segment::Model;
pub use entity_api::transcript_segment::find_by_transcription;
